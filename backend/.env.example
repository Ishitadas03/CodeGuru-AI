# ─── Backend Environment Variables ─────────────────────────────────────────────
# Copy this file to .env and configure for your environment.
#
# Render Deployment:
#   Set these in Render Dashboard → Environment tab
#   Render auto-generates DATABASE_URL for PostgreSQL services.
# ────────────────────────────────────────────────────────────────────────────────

# Core Security (REQUIRED in production)
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS

# Environment
ENVIRONMENT=development

# PostgreSQL (Render auto-provides DATABASE_URL for managed PostgreSQL)
# For local dev, use SQLite: SQLALCHEMY_DATABASE_URI=sqlite+aiosqlite:///./codeguru.db
# For Render, set DATABASE_URL or individual vars below:
POSTGRES_SERVER=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=codeguru

# Redis (optional — for caching and Celery)
REDIS_HOST=localhost
REDIS_PORT=6379

# AI Providers (at least one required in production)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GEMINI_API_KEY=
OLLAMA_HOST=http://localhost:11434

# Google OAuth
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Stripe Billing
STRIPE_SECRET_KEY=sk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME
STRIPE_PRO_PRICE_ID=price_REPLACE_ME
